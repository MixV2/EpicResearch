# Get System File
Returns a file stored in the MCP's cloudstorage.

## Request
| URL | Method |
| - | - |
| /:namespace/api/cloudstorage/system/:uniqueFilename | `GET` |

## Parameters
- `uniqueFilename` - unique name of the file to access